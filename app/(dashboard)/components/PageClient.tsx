"use client";

import { IProduct } from "@/app/actions/getProducts";

type Props = {
    products: IProduct[];
};

export default function PageClient({products}: Props) {

    return (
        <>
        </>
    );
}